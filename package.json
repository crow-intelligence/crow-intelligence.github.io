{
  "name": "crow-intelligence-site",
  "version": "1.0.0",
  "private": true,
  "description": "Tests for the site's cookie-consent banner and analytics loader (consent.js). The site itself is built with Pelican; Node is only used for these tests.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vitest": "^4.1.10"
  }
}
